export {
  buildIndexName,
  getIdentifierNameMaxLength as getIndexNameMaxLength,
  truncateIdentifierName as truncateIndexName,
} from '@ddlbuilder/ddl-core';
export {
  DEFAULT_IDENTIFIER_NAME_MAX_LENGTH as MAX_INDEX_NAME_LENGTH,
  ORACLE_IDENTIFIER_NAME_MAX_LENGTH as ORACLE_INDEX_NAME_LENGTH,
} from '@ddlbuilder/ddl-core';
